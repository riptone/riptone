{
  "name": "tone-profile",
  "private": true,
  "type": "module",
  "description": "Generates the README, its stats card and the profile picture.",
  "scripts": {
    "generate": "bun run scripts/generate.ts",
    "avatar": "bun run scripts/avatar.ts",
    "vendor": "bun run scripts/vendor.ts",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.4.1",
    "typescript": "^7.0.2"
  }
}
